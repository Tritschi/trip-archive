# Contributing

Never publish private GPS tracks, access tokens or unredacted entity data.

Storage format changes require a schema decision, migration, tests and a changelog entry.

Run tests with:

```bash
python -m pytest
```
